name: Smasfinland Robot Framework Browser Tests
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
jobs:
  robot_test:
    runs-on: docker://marketsquare/robotframework-browser:latest
    steps:
      - name: Robot Framework 
        run: robot -o NONE -l NONE -r NONE robot/SmashfinlandRobot
